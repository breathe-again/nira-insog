# Keep this folder in the repo (for path stability), but ignore everything in it.
# Uploaded files and DB backups live here on the developer's machine and are
# never committed.
*
!.gitignore
!README.md
